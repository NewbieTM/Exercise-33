#33. Дан двумерный массив символов, состоящий из 10 строк и 10 столбцов.
# Заполнить массив двумя типа символов: звездочкой ('*') и пробелом (' ') так,
# чтобы выше главной диагонали и на диагонали находились звездочки, а ниже главной диагонали – пробелы.
# Вывести массив на экран в виде таблицы. Должен быть получен рисунок вида:


range_of_matrix = 10
arr = [[] for _ in range(range_of_matrix)]
i = 11
c = 0
while i > 1:
    for c in range(i-1):
        arr[c].append('*')
        c += 1
    i -= 1
    c = 0


for c in range(1,10):
    while c < 10:
        arr[c].insert(0,' ')
        c+=1


for row in arr:
    print(row)
